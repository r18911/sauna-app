<%= include_gon %>

<%= form_with  model: @payment, url: reservation_payments_path(@reservation), id: 'charge-form', class: 'card-form',local: true do |f| %>
  <%= render 'layouts/error_messages', model: @payment %>
  <div class='form-wrap'>
    <%= f.label :amount, "金額" %>
    <%= f.text_field :amount, class:"amount", placeholder:"例)2000" %>
  </div>
  <div class='form-wrap'>
    <p>カード番号</p>
    <div id="number-form" class="input-default"></div>
  </div>
  <div class='form-wrap'>
    <p>期限</p>
    <div id="expiry-form" class="input-default"></div>
  </div>
  <div class='form-wrap'>
    <p>カード背面4桁もしくは3桁の番号</p>
    <div id="cvc-form" class="input-default"></div>
  </div>
  <%= f.submit "購入", class:"button", id: "button" %>
<% end %>